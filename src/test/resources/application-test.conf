uservice-party-process {
  port = 8088
  platform.roles {
    manager: "admin"
    delegate: "admin"
    operator: "security, api"
  }
  storage {
    type = "File"
    container = "local"
    endpoint = "local"
    application {
      id = "local"
      secret = "local"
    }
  }
}