uservice-party-process {
  port = 8088
  mail-template-path = "src/test/resources/mail-template.json"
  product.roles {
    manager: "admin"
    delegate: "admin"
    operator: "security, api"
  }
}

services {
   party-management = "localhost"
   party-proxy = "localhost"
   attribute-registry = "localhost"
   authorization-process = "localhost"
   user-registry-management = "localhost"
}

pdnd-interop-commons {
  storage {
    type = "File"
    container = "local"
    endpoint = "local"
    application {
      id = "local"
      secret = "local"
    }
  }

  mail {
    sender = "pdnd@pagopa.it"
    smtp {
      user = "pippo@baudo.it"
      password = "Kat1a_I_love_U"
      server = "smtp.gmail.com"
      port = 587
    }
  }
}